import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.knittingtracker',
  appName: 'Вязальный дневник',
  webDir: 'dist'
};

export default config;
